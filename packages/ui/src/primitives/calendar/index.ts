export { Calendar, type CalendarProps } from './calendar';
// Re-export common react-day-picker types so apps type selections without a direct dependency.
export type { DateRange, Matcher } from 'react-day-picker';
