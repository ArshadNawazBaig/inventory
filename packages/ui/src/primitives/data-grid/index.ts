export { DataGrid, type DataGridProps } from './data-grid';
// Re-export the TanStack column type so consumers type columns without a direct dependency.
export type { ColumnDef } from '@tanstack/react-table';
