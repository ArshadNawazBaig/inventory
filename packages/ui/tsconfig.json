{
  "//": "Typecheck config — covers source + tests (excludes stories; Storybook installed later).",
  "extends": "@stockflow/tsconfig/react-library.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src", "vitest.config.ts", "vitest.setup.ts"],
  "exclude": ["node_modules", "dist", "src/**/*.stories.tsx"]
}
